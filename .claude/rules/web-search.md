# Web Search

When searching the web, use every available search capability in parallel — built-in tools, MCP servers, and skills alike. Do not pick one; fan out across all of them.

- Issue the same query (or close variants) simultaneously to broaden coverage
- Vary phrasing, keywords, and specificity across the parallel calls to surface different results
- Consolidate findings afterwards; deduplicate overlapping sources
