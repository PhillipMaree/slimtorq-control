# Readable Code

Prefer readable code; avoid comments. Only comment the *why* when it is genuinely non-obvious — a hidden constraint, a workaround, a subtle invariant. Names and structure should explain the *what*.
