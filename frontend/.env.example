# Copy to .env.local for `npm run dev`.
# Production builds (static export served by FastAPI) ignore this — the client
# uses the relative path `/api` and hits FastAPI on the same origin.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api
